# Frontend

Run
- Copy `.env.example` to `.env` and set Firebase and API URL
- From repo root: `npm install` then `npm start`

Notes
- Uses React Router v6 and Material-UI
- Auth via Firebase; token sent as Bearer to backend
